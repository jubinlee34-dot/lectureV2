# AGENTS.md

## 기본 작업 원칙

- 작업 전에 현재 요청의 범위와 영향을 확인한다.
- 사용자가 요청하지 않은 기능, 파일, 설정을 임의로 변경하지 않는다.
- 데이터베이스 스키마, 환경변수, 인증, 보안, 배포 흐름 변경은 특히 신중하게 다룬다.
- commit, push, PR, merge는 사용자의 명시적 요청 없이 임의로 진행하지 않는다.
- 작업 완료 전 가능한 범위에서 build, test, diff를 확인한다.
- 최신 GitHub 상태와 Notion 기록이 충돌하면 GitHub의 실제 코드 상태를 사실 기준으로 삼고 차이를 보고한다.

---

## Notion 기반 작업 관리

### 작업 관리 위치

lectureV2의 작업 우선순위와 진행 상태는 아래 Notion 페이지에서 관리한다.

- 상위 작업 기록 페이지: `클로드 코드 작업 리스트`
- URL: `https://app.notion.com/p/3cebaa25b9688011aa21e8351e5b9767`

lectureV2 관련 작업은 위 페이지의 `lectureV2` 하위 페이지와 그 안의 최신 작업 리스트를 우선 확인한다.

---

## Notion 읽기 규칙

사용자가 다음과 같은 의미의 요청을 하면 별도 확인 없이 관련 Notion 작업 리스트를 읽을 수 있다.

- `다음 작업 진행`
- `다음 작업 찾아줘`
- `작업 리스트 확인`
- `노션 기준으로 진행`
- `작업 시작`
- `우선순위 확인`
- `이어서 작업해`

Notion 내용을 읽지 않고 과거 대화나 AI의 기억만으로 다음 작업을 결정하지 않는다.

Notion은 작업 계획과 결정 기록의 기준이며, GitHub는 실제 구현 상태의 기준이다.

---

## 작업 시작 절차

### 1. Notion 작업 리스트 확인

현재 lectureV2 작업 리스트에서 다음을 확인한다.

- 진행 중 작업
- 완료 작업
- 우선순위
- 보류 사항
- 사용자 승인 필요 사항
- 이전 작업의 다음 단계

### 2. GitHub 기준점 확인

작업 시작 전에 반드시 가능한 범위에서 다음을 확인한다.

- 현재 branch
- HEAD commit
- `origin/main`
- working tree 상태
- 열린 PR
- 최근 main 변경

Notion에 기록된 기준 commit과 실제 GitHub 상태가 다르면 그대로 진행하지 않는다.

차이를 확인하고 현재 GitHub 상태를 기준으로 다시 판단한다.

### 3. 조사

바로 코드를 수정하지 않는다.

다음 작업 후보에 대해 먼저 조사한다.

- 관련 코드
- 이미 구현된 기능인지
- 현재 오류 또는 누락
- 영향 파일
- DB / migration 영향
- 환경변수 영향
- 인증 / 개인정보 / 보안 영향
- 외부 API 영향
- 기존 기능 회귀 가능성

### 4. 우선순위 재판단

Notion의 기존 순서를 무조건 실행하지 않는다.

현재 코드 상태를 기준으로 다음 순서로 위험도와 중요도를 다시 판단한다.

1. 개인정보 / 보안 / 데이터 손실 위험
2. 기능 오류
3. 상업화에 필수적인 기능
4. 외부 서비스 연동
5. 사용성 / UX
6. 디자인 / 시각 개선

기존 우선순위를 변경할 필요가 있으면 다음을 보고한다.

- 기존 우선순위
- 새로 발견한 사실
- 변경을 권하는 이유
- 대안

---

## 작업 계획 작성

조사 후 실제 구현 전에 다음 내용을 정리한다.

1. 작업 목적
2. 현재 문제
3. 구현 범위
4. 변경 예상 파일
5. 변경하지 않을 영역
6. DB / migration 여부
7. 보안 / 개인정보 영향
8. 검증 방법
9. 브라우저 E2E 항목
10. 완료 조건

복잡하거나 위험도가 높은 작업은 구현 전에 사용자에게 계획을 보고한다.

---

## 승인 경계

다음 작업은 사용자의 승인 없이 실행하지 않는다.

- DB schema 변경
- migration 실행
- production DB 변경
- 환경변수 이름 변경
- 인증 방식 변경
- OAuth scope 확대
- 보안 정책 변경
- 개인정보 처리 구조 변경
- commit
- push
- PR 생성
- merge
- 배포

사용자가 특정 작업에 대해 이미 명시적으로 승인한 범위는 다시 묻지 않는다.

---

## 구현 규칙

승인된 작업은 다음 원칙으로 진행한다.

- 최신 `main`을 기준으로 작업한다.
- 필요하면 새 branch를 만든다.
- 한 작업에는 하나의 명확한 목적을 둔다.
- diff를 작게 유지한다.
- 관련 없는 리팩터링을 함께 하지 않는다.
- 기존 UI와 기능을 불필요하게 변경하지 않는다.
- 기존 DB 운영 규칙을 우선한다.

특히 신규 `public` 테이블을 만드는 경우 `docs/supabase-db-operations.md`를 반드시 확인한다.

---

## 필수 검증

코드 변경 후 최소 다음을 확인한다.

- `pnpm run check`
- `pnpm run build`
- `git diff --check`

기능에 따라 추가로 확인한다.

- 브라우저 E2E
- 로그인 / 로그아웃
- 데이터 생성 / 수정 / 삭제
- 오류 경로
- DB migration history
- RLS / ACL
- console error

검증하지 못한 항목은 통과했다고 기록하지 않는다.

---

## Notion 작업 기록 규칙

### 기록 실행 조건

Notion **읽기**와 **쓰기**를 구분한다.

작업 리스트를 확인하기 위한 Notion 읽기는 위의 작업 시작 조건에 따라 자동으로 수행할 수 있다.

Notion 페이지 생성·수정은 사용자가 다음과 같이 요청한 경우 수행한다.

- `노션 기록`
- `노션에 기록해줘`
- `작업 기록 남겨줘`
- `노션 기준으로 작업하고 기록해줘`
- 그와 동일한 의미의 요청

사용자의 요청 없이 기존 Notion 기록을 임의로 수정하거나 삭제하지 않는다.

### 기존 작업 우선

- 현재 작업과 관련된 기존 하위 페이지가 있으면 새 페이지를 먼저 만들지 않는다.
- 같은 작업의 연속이면 기존 페이지에 이어서 기록한다.
- 별개의 작업이거나 적절한 기존 페이지가 없으면 새 하위 페이지를 만든다.

### 새 페이지 제목

`YYYY-MM-DD_상태_작업명`

예:

`2026-09-05_진행중_LectureV2 Google 로그인`

### 기록 내용

1. 작업 목적
2. 조사 결과
3. 이번에 수행한 작업
4. 주요 결정사항
5. 변경 또는 생성한 파일
6. 테스트 및 검증 결과
7. 발견된 오류와 리스크
8. 현재 상태
9. 다음 작업

### 코딩 작업 추가 기록

가능한 경우 다음도 기록한다.

- 저장소
- 기준 commit
- branch
- 주요 변경 파일
- commit
- PR
- CI
- build / test 결과
- 미해결 이슈

### 기록 원칙

- 대화 전체를 복사하지 않는다.
- 인수인계에 필요한 핵심만 기록한다.
- 완료 / 진행중 / 보류를 구분한다.
- 실패한 작업과 미해결 오류를 누락하지 않는다.
- 기존 기록을 임의로 삭제하거나 덮어쓰지 않는다.

---

## 기본 에이전트 실행 루프

사용자가 `노션 작업 리스트 기준으로 다음 작업 진행`이라고 요청하면 기본적으로 다음 순서를 따른다.

**Notion 확인 → GitHub 기준점 확인 → 조사 → 우선순위 판단 → 작업 계획 보고 → 승인된 범위 구현 → 검증 → 결과 보고 → 요청된 경우 Notion 업데이트**

DB, 인증, 개인정보, 보안, migration이 관련된 경우 조사 단계와 승인 경계를 생략하지 않는다.
